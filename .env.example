# Copy to .env and fill in. NEVER commit .env (gitignored).
FIREWORKS_API_KEY=

# Optional overrides (defaults in src/styleforge/config.py) — all-Gemma by design
# FIREWORKS_BASE_URL=https://api.fireworks.ai/inference/v1
# PERCEPTION_MODEL=accounts/fireworks/models/gemma-3-27b-it
# STYLIZER_MODEL=accounts/fireworks/models/gemma-3-27b-it
# JUDGE_MODEL=accounts/fireworks/models/gemma-3-27b-it
# JUDGE_ENSEMBLE=model_id_1,model_id_2   # eval/rerank tier; single JUDGE_MODEL if unset
# WHISPER_MODEL=tiny
# MAX_FRAMES=12
# STYLEFORGE_MOCK=0   # 1 = offline smoke test, no API calls
